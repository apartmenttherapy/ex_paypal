defmodule ExPaypal.API.Referral do
  @moduledoc """
  Logic for making and processing referral calls (`/v1/customer/partner-referrals`)
  """

  alias ExPaypal.API.Request

  def onboard(seller, config) do

  end
end
